{
  "name": "my-js-action",
  "version": "1.0.0",
  "description": "A custom JavaScript action",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/core": "^1.9.1"
  }
}

